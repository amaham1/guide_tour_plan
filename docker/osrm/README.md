Place a preprocessed OSRM dataset in this folder before starting `docker-compose`.

Expected file:
- `jeju-latest.osrm`

You can prepare it with the standard OSRM pipeline:
- `osrm-extract`
- `osrm-partition`
- `osrm-customize`
